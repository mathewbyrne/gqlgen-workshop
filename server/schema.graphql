
type Query {
    character(id: Int!): Character!
    characters(page: Int! = 1): [Character!]!
    likes:[Character!]!
}

type Character {
    id: Int!
    name: String!
    species: String!
    image: String!
    isLiked: Boolean!
}

type Mutation {
    like(id: Int!): [Character!]!
}
